import { ConditionToIconPipe } from './condition-to-icon.pipe';

describe('ConditionToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
